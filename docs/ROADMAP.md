# 로드맵 — M0부터 공개까지 38주

주 8–10시간(평일 저녁 + 주말 반나절) 기준. 각 마일스톤 끝에 **아들이 손에 쥐고 해볼 수 있는 빌드**. 완료 기준을 만족하기 전에 다음으로 가지 않는다. 2주마다 플레이테스트 → `PLAYTEST-LOG.md`.

| M | 이름 | 기간 | 완료 기준 |
|---|---|---|---|
| **M0** | 기술 검증과 조작 실험 | 2주 | 아들이 폰·PC 양쪽에서 설명 없이 5분 안에 블록을 부수고 쌓는다. PC 60fps, 중급 폰 30fps+ |
| M1 | 월드의 뼈대 (싱글) | 4주 | 브라우저를 껐다 켜도 아들이 만든 집이 그대로 있다 (IndexedDB) |
| M2 | 멀티 코어 — 마을 | 5주 | 아빠는 PC로, 아들은 폰으로 같은 마을에 집을 짓는다. 폰을 잠갔다 켜도 그 자리 |
| M3 | 원정 루프 — 게임의 심장 | 5주 | 둘이 원정을 다녀와서 "한 번 더 갈까?"가 나온다. 원정 시간 값 확정 |
| M4 | 가방·조합·채팅 | 4주 | 아들이 JSON을 고쳐 새 레시피를 만들고, 채팅 문구 20개 확정. 양조기에서 물병 → 어색한 물약 → 신속의 물약이 나온다(`potions.json`, 결정 #61) |
| M5 | 가족 연결 — 계정·할 일·시간 | 4주 | 아들이 할 일 체크 → 아빠가 폰(또는 게임 내 HUD)에서 승인 → 한 판 더 열린다. 월요일 정산 확인. **모든 플레이어 닉네임 + PIN 이어하기**(친구 포함, #63): 다른 폰에서 이름 + PIN 을 넣으면 내 가방·드래곤이 그대로 |
| M6 | 마을 성장 + 드래곤 + 경험치 | 5주 | 2주 뒤 접속한 친구가 "마을 커졌다"고 먼저 말한다. 아들이 경험치 레벨을 써서 첫 드래곤 알을 부화시키고, 안장을 얹어 타고, 빔을 쏜다 |
| M7 | 원정의 위험 + 보스 | 4주 | 남은 3분에 "돌아가자!"가 채팅에 찍힌다. 죽으면 구슬이 떨어지고 되찾을 수 있다. 아빠와 아들이 거미 왕을 잡고, 우민 방어전을 한 번 막아낸다. 엔더 드래곤 전투 기본형 (일정 밀리면 방어전을 v1.1로) |
| M8 | 마감 | 3주 | 처음 보는 아이가 링크만 받고 30초 안에 들어와 첫 원정을 끝낸다 |
| M9 | 공개(가족·친구) | 2주 | 아들이 친구 다섯에게 코드를 보내고, 여섯 명이 함께 원정을 다녀온다 |

M5는 게임 엔진과 거의 독립적인 API·DB 작업 → 순서 조정 가능, 아빠 전문 영역이라 가장 빨리 끝날 가능성. M2·M3을 앞에 둔 것이 핵심 결정(멀티는 나중에 못 붙인다, 원정이 심장이다).

---

## M0 작업 분해 (2주)

목표: **프레임이 나오는가, 조작이 되는가.** 지형·서버·저장 없음.

### 0. 저장소 골격 (0.5일) — 완료 2026-09-12
- [x] pnpm workspaces: `packages/shared`, `packages/client`, `packages/server`(빈 껍데기)
- [x] TypeScript strict 공통 tsconfig, ESLint(`Math.random` 금지 규칙 포함), vitest
- [x] `data/` 로더 + zod 스키마(`blocks.json`만 우선), 실패 시 한국어 에러 (`shared/rules/blocks.ts`)
- [x] Vite 개발 서버 `--host`. HTTPS 는 `pnpm dev:https`(자체 서명) — 결정 #40

### 1. 청크와 메싱 (3일) — 완료
- [x] `shared/chunk`: `Chunk` (Uint16Array 4096 + 팔레트), get/set, `VoxelWorld` + 18³ 패딩 빌더
- [x] `client/workers/mesher.worker.ts`: greedy meshing + 정점 AO, 불투명 인접 면 제거, ArrayBuffer transfer
- [x] vitest: 단일 블록 = 6면 24정점, 2×2×2 꽉 찬 큐브 = 6면, 이웃 청크 경계 면 처리 (+ 와인딩·AO·유리·물)
- [x] 테스트 월드: 8×8×4 청크 광장 + 완만한 언덕 + 연못 + 나무 + 계단 + 기둥 + 집 뼈대 + 동굴 (하드코딩)
- 벤치(`pnpm exec vitest bench --run`, 2코어 VM): 지표면 청크 평균 4.6ms, 꽉 찬 청크 2.9ms, 체커보드 최악 20ms. 실기기에서 재측정

### 2. 렌더 (2일) — 완료
- [x] Three.js 씬, 커스텀 `ShaderMaterial`(면 음영 + AO, 하늘빛 상수, 안개). 조명 flood fill 은 M1
- [x] `DataArrayTexture` 로더: `textures/*.png` 자동 수집 → 레이어. 임시 텍스처 44종 생성기 `pnpm textures`
- [x] 프러스텀 컬링, 청크 관리자(재메싱 큐 거리 우선, 프레임당 상한 2, 초기 로딩 burst)
- [x] FPS·드로우콜·메싱 시간 디버그 오버레이 (PC F3 / 폰 `i` 버튼)
- [x] 덤: 하늘 돔 + 해, 손에 든 블록(흔들림·휘두르기), 부수기 금 10단계, 등각 블록 아이콘 핫바

### 3. 조작 (3일) — 구현 완료, 실기기 검증 남음
- [x] `InputState` 공통 타입 + `InputManager`(장치 합산)
- [x] `touch.ts`: 왼쪽 반 터치 → 스틱 생성(엄지 위치), 오른쪽 드래그 → 시점, 탭=놓기/220ms 홀드=부수기(게이지 링), 점프·웅크리기 버튼, `touch-action:none`
- [x] `keyboard.ts` + Pointer Lock: WASD/Space/Shift/Ctrl/마우스, 좌클릭 홀드·우클릭(유지 시 반복), 1~9·휠 슬롯. 잠금 불가 환경 열화 모드
- [x] `gamepad.ts`: 표준 매핑
- [x] 1인칭 카메라, AABB 0.6×1.8×0.6 스윕 충돌(`shared/physics`), 중력·점프·웅크리기 낙하 방지·간이 수영
- [x] 레이캐스트(Amanatides–Woo, 5블록) + 조준 블록 외곽선 + 중앙 십자선
- [x] 블록 부수기(홀드 시간 = `blocks.json` hardness, 부순 뒤 0.3초 대기) / 놓기(몸과 겹치면 금지), 슬롯 바 9종
- [ ] 아빠 PC 에서 마우스 감도·점프 느낌 확인 후 상수 조정 (`Player.ts` 상단)

### 4. 폰 마무리 (1.5일) — 실기기 남음
- [x] 전체화면 API(+가로 잠금 시도), 가로 모드 안내 토스트, `100dvh`, safe-area
- [x] 해상도 스케일 자동 조절(2초 평균 36ms 초과 시 -0.25, 여유 시 복귀). 렌더 거리 폰 5 / PC 8
- [ ] iOS Safari 주소창·전체화면 불가 확인 → 필요하면 '홈 화면에 추가' 안내
- [ ] 아이폰 + 갤럭시 중급기 실측: fps, 메싱 ms, 발열 10분 → `PLAYTEST-LOG.md`

### 5. 플레이테스트 (반나절)
- [ ] 아들에게 폰·PC 각각 주고 설명 없이 5분. 관찰 기록 → `PLAYTEST-LOG.md`
- [ ] 합격 기준 미달 시 조작 재설계 → M0 연장. 통과 시 M1.

### M0에서 하지 않는 것
지형 생성, 조명 flood fill, 서버, 저장, UI 메뉴, 사운드, 드래곤.

---

## M1 작업 분해 (4주) — 착수 2026-09-13

목표: **브라우저를 껐다 켜도 아들이 만든 집이 그대로 있다.** 순서는 체감 큰 것부터.

### 1. 저장 (1주) — 완료 2026-09-13
- [x] `shared/chunk/serialize`: 문자열 id 팔레트 + RLE (결정 #39·#56). 테스트 6개
- [x] `client/save/WorldStore`(IndexedDB) + `SaveManager`: 바뀐 청크만, 1.5초 디바운스·20초 주기·탭 숨김/닫힘 시 즉시 저장. 플레이어 위치·시점 저장
- [x] 접속 시 테스트 월드 생성 → 저장 청크 덮어쓰기 → 위치 복원. 지형 버전(`TEST_WORLD_GEN_VERSION`)이 다르면 저장 폐기 + 안내
- [x] '처음 세계로 되돌리기'(게임 방법 창), 저장 불가 브라우저(시크릿 등) 안내, 디버그 오버레이에 저장 상태
- [ ] 폰 실기기: 홈 화면 앱·사파리·크롬에서 껐다 켜도 남는지 (IndexedDB 는 브라우저별로 따로)

### 2. 조명 (1~1.5주) — 구현 완료 2026-09-13, 실기기 확인 남음
- [x] 스카이라이트·블록라이트 0–15 flood fill (`shared/light/lightEngine.ts`). 세계 전체 평면 배열(블록당 1바이트, 두 채널 니블), 결정론. 증분 갱신 = 전체 재계산을 테스트로 강제(무작위 200회). 테스트 15개
- [x] 메싱 워커 통합: 18³ 빛 조각을 블록 조각과 함께 transfer, 꼭짓점 빛 = 면 바깥 4칸 평균(불투명 제외) → `meta.w` → 셰이더에서 면 음영·AO 와 곱. 횃불·발광석·용암(`lightEmit`) 발광, 물·나뭇잎·얼음은 `lightFilter` 1 로 한 칸마다 어두워짐
- [x] 블록 변경 시 국소 재계산(걷어내기 BFS → 가장자리에서 다시 채우기) + 빛이 바뀐 청크(이웃 포함) 재메싱. 프레임마다 한 번 `flush`. 액체 흐름(용암 발광·물 차단)도 반영
- [x] 낮·밤 배율 `uSkyLight` 는 스카이 채널에만 곱한다(값은 M3 의 원정 타이머가 바꾼다). 최소 밝기 0.05 — 완전 암흑 없음
- [x] 핫바 8번을 모래 → 발광석으로(조명 확인용)
- [ ] 폰 실기기: 동굴·집 안이 어둡게 보이는지, 발광석을 놓으면 주변이 밝아지는지, `i` 의 메싱 ms 가 M0 대비 얼마나 늘었는지

### 3. 마을 터 생성기 (1.5주) — 구현 완료 2026-09-13, 실기기 확인 남음
- [x] `shared/worldgen/village.ts`: mulberry32 + simplex-noise. 128×128×128(8×8×8 청크), 광장 높이 40. 광장(조약돌 원·길) + 포탈 자리(조약돌 단·흑요석 문틀 4×5) + 강(서→동, 광장 북쪽을 돌아감, 폭 4~9·깊이 1~3·모래 강변) + 큰 밭 둘(농지·물길·호박·수박·건초) + 집 뼈대 + 참나무 둘레 + 가장자리 언덕 + 땅속(기반암·광물 7종·굴 동굴·동남쪽 동굴 입구). 청크마다 4096 배열을 통째로 넣어 2코어 VM 208ms
- [x] 스냅샷 테스트: 같은 시드 → 같은 지문(블록 **문자열 id** 해시라 blocks.json 순서 무관), 다른 시드 → 다름, 기본 시드 지문 고정. 배치 검사 10개(물 옆이 막혀 있는지 등). 테스트 11개
- [x] 하드코딩 테스트 월드 삭제, 저장 키 `village` + `VILLAGE_GEN_VERSION`. 옛 `test-world` 저장은 접속 시 지우고 안내
- [x] 스폰 = 광장 한가운데 돌 위, 북쪽(포탈)을 본다. 연못 → 강
- [x] `farmland`(농지) 블록·텍스처 추가. 조명 초기 계산 빠른 길(기둥 직사광 + 경계 씨앗만 BFS): 2배 큰 세계에서 110ms
- [x] 실기기 수치(아빠, 2026-09-13): 첫 로딩 1초 이내, 광장 60fps, 지형 생성 61ms, 조명 27ms → `PLAYTEST-LOG.md`
- [ ] 실기기: 밭·강·언덕·동굴이 보이는지, 저장이 새 세계에서도 되는지, 아들 폰 수치

### 4. 마무리 (반나절) — 준비 완료 2026-09-13, 아들 이틀 테스트 대기
- [x] M1 완료 기준 점검 순서를 `PLAYTEST-LOG.md` 에 준비 (1일차 집 짓기·물길·동굴·`i` 값 → 폰 끄기 → 2일차 확인)
- [x] 아빠 실측·피드백 반영: 새 세계 첫 로딩 1초 이내·60fps, 물길에서 못 나오는 문제(물속 둑 오르기)
- [ ] 아들 플레이테스트: 집 짓기 → 폰 끄기 → 다음 날 켜기 → 그대로인가. **같은 폰·같은 브라우저(또는 홈 화면 앱)** 로 — 저장은 브라우저별
- [ ] `PLAYTEST-LOG.md` 기록, 통과 시 M2. M2 시작 전 결정: 서버를 어디에 띄우나(IDC PC 포트 개방 / 별도 호스팅), 마을 코드 형식

## M2 작업 분해 (5주) — 착수 2026-09-18

목표: **아빠는 PC로, 아들은 폰으로 같은 마을에 집을 짓는다. 폰을 잠갔다 켜도 그 자리.** 서버가 세계의 진실(규칙 1). M1-4 아들 이틀 테스트는 아직 미기록 — 아빠 지시로 M2 를 먼저 착수(저장이 서버로 옮겨 가므로 M2 빌드에서 같은 테스트를 한다).

호스팅: 이 IDC PC(공인 115.68.221.179). **이미 열려 있는 5173 포트를 게임 서버가 쓴다**(클라 빌드 + WebSocket `/ws`). 개발용 Vite 는 5174 (`/ws` 를 5173 으로 프록시). 결정 #60.

### 1. 프로토콜 (`shared/protocol`) — 완료 2026-09-18
- [x] `ByteWriter/ByteReader`(LE), 바이너리 메시지 코덱: PlayerMove·PlayersState·BlockChangeReq/Changed/Rejected·BlockBatch(액체)·ChunkData(청크 blob)·Ping/Pong. 블록은 **문자열 id**(#39). 라운드트립 테스트 9개
- [x] JSON 로비: hello(토큰)·join(닉·색·6자리 코드)·create → welcome(시드·스폰·플레이어 목록) → ChunkData… → ready. 닉네임 정리, 코드 형식

### 2. 서버 뼈대 (`packages/server`, Node 22 + ws + better-sqlite3, tsx 로 실행) — 완료 2026-09-18
- [x] HTTP 정적(클라 `dist`) + 같은 포트 WebSocket `/ws` + `/health`. 마을 룸: `generateVillage(seed)` + 저장 청크 덮어쓰기, 20Hz 틱(액체 시뮬 → BlockBatch, 위치 브로드캐스트)
- [x] 블록 변경 검증: 도달 6.5칸, 초당 12회, 기반암·모르는 블록·내부 블록 거절, 누가 서 있는 칸 거절 → 적용 → 전원에 BlockChanged. 거절 사유는 한국어로 클라에
- [x] SQLite: villages / chunk_diffs(M1 직렬화 재사용) / players(토큰·위치). 바뀐 청크만 30초마다·마지막 퇴장·종료 시 저장. 하루 1회 백업(7개 보관)
- [x] 기본 마을 "드래곤 빌리지" 자동 생성(시드 상수, 코드 `DV_DEFAULT_CODE` = 482913), 새 마을 만들기(새 시드·코드). 최대 6명, 같은 토큰 재입장 시 예전 연결 끊기, 틀린 코드 5번이면 끊기
- [x] 테스트 12개: 검증 규칙 전부, 액체 틱 → BlockBatch, 저장 라운드트립(메모리 DB), 재시작 후 불러오기·입장 시 ChunkData, 지형 버전 불일치 폐기

### 3. 클라이언트 — 완료 2026-09-18
- [x] 로비: 이름·색(16)·마을 코드(주소 `?code=` 로 미리 채움) → 입장 / 새 마을. 토큰은 `localStorage`(`?fresh` 면 탭 전용). 끊기면 "다시 연결" 오버레이
- [x] `NetClient`: welcome 의 시드로 세계 생성 → ChunkData 덮어쓰기 → 조명 → 시작. 20Hz 위치 전송, 블록은 낙관적 적용 + 거절 시 되돌리기(+한국어 토스트), 남의 변경·액체 묶음 반영(+조명 갱신)
- [x] 다른 플레이어: 색 있는 복셀 인형(걷기·웅크리기 애니) + 머리 위 이름, 지수 보간
- [x] IndexedDB 저장·클라 액체 시뮬 제거(서버가 진실). 게임 방법 창에 마을·코드·인원 표시
- [x] Pages 빌드는 서버 주소 안내 페이지 역할만 (`VITE_SERVER_URL`, HTTPS 페이지에서 ws:// 는 막힘)
- [x] 패널 검증: 두 탭(아빠·아들) 동시 접속, 서로의 인형·이름 표시, 한쪽이 놓은 블록이 다른 쪽 세계에 반영, 서버 저장 1회

### 4. 운영·마무리
- [x] 이 PC 에서 서버 실행(5173) — 클라 빌드를 서버가 서빙. **작업 스케줄러 `DragonCraftServer` 등록 2026-09-19**(창 없이, 로그온 30초 후 + 5분마다 감시, `tools/win/start-server.ps1`)
- [x] 외부(집)에서 http://115.68.221.179:5173 접속 확인 — 2026-09-19 아빠 확인
- [x] 아빠 PC + 아들 폰 동시 접속 → 서로 보이고 같은 블록이 보이는지, 폰 잠갔다 켜도 그 자리인지(완료 기준) — **2026-09-19 아빠 확인, 셋 다 됨 → M2 통과**. 발견: 같은 폰의 다른 브라우저/새 창 + 같은 이름 = 플레이어 2명(계정 = 브라우저 저장 토큰이라서, `PLAYTEST-LOG.md` 2026-09-19)
- [ ] 통과 시 M3(원정 루프). 원정지 6곳과 드래곤 16종 재료는 아들 1차 답변으로 이미 확정(`expeditions.json`, `dragons.json`) — M3 는 바로 시작할 수 있다. 남은 확인은 나무 드래곤 묘목 1개(그림) vs 2개(답변)

## M3 작업 분해 (5주) — 착수 2026-09-19

목표: **둘이 원정을 다녀와서 "한 번 더 갈까?"가 나온다. 원정 시간 값 확정.** 마을 포탈 → 새 시드의 초원 섬 → 10분(낮→밤) → 포탈로 귀환 → 정산 창. 서버가 원정 세계·타이머·정산의 진실(규칙 1). 원정지는 저장하지 않는다(DESIGN 3절).

M3 에서 하지 않는 것: 가방·인벤토리(M4 — 정산은 "모은 블록 수"로), 체력·몹·밤의 위험(M7 — 밤은 어두워지기만), 포탈 단계 해제(M6 — 초원 섬 하나만), 시간 규칙(M5).

### 1. 원정지 데이터·규칙 (`shared/rules/expeditions.ts`) — 완료 2026-09-19
- [x] `data/expeditions.json` zod 로더(한국어 에러): durationSec·nightStartsAt·treasures·materials·release, 파일 상단 returnGraceSec·failedReturnKeepRatio·minStartMarginMin. 낮·저녁·밤 단계 함수 `phaseAt(sec)` 와 스카이라이트 배율 `skyLightAt(sec)`(순수 함수, 클라 하늘·서버 위험도 공용)
- [x] 테스트: 실제 파일 통과, v1 6곳 id, 단계·배율 경계값 (저녁 = 밤 90초 전, 밤 배율 0.22)

### 2. 초원 섬 생성기 (`shared/worldgen/island.ts`) — 완료 2026-09-19
- [x] 256×256×128(16×16×8 청크). 원형 마스크 × 노이즈 높이맵, 바다(자연 물, 수면 고정)·모래 해변·잔디 언덕, 참나무 숲, 물가 사탕수수·호박·수박 밭 자리, 돌 속 석탄·철, 작은 언덕 굴
- [x] 보물 상자 3개: 돌 오두막(상자 블록) 시드 배치, 도착 포탈(흑요석 문틀) 섬 가운데 + 스폰
- [x] 지문 스냅샷 테스트 + 시드가 다르면 다른 섬 + 생성 시간 — **2코어 VM 290~360ms**(875 청크, 283만 블록). 폰 실측은 5절에서
- [x] 동물·몹은 M7. 새 블록 없이 기존 blocks.json 으로(사탕수수·호박·수박·상자·발광석 이미 있음)

### 3. 서버 원정 룸 (`packages/server/src/expedition.ts`) — 완료 2026-09-19
- [x] `Expedition`(마을 룸 안의 서브 월드, `RoomPlayer.world` 로 구분): 마을 룸 안의 임시 서브 월드. 시작 요청(포탈 앞 누구나, M3) → 시드(crypto) → `generateIsland(seed)` → 참가자 이동(WorldEnter). 진행 중 합류 가능. 블록 변경 검증은 마을과 같되 보호 구역 없음, 저장 없음. 액체 틱 동일
- [x] 타이머: durationSec, 1Hz `ExpeditionTimer`(서버 시각·끝나는 시각·단계). 끝나면 전원 마을 스폰으로(WorldLeave). 포탈 안으로 들어가면 개인 귀환. `returnGraceSec` 뒤 세계 폐기
- [x] 정산: 플레이어별 "부순 블록 → 드롭 아이템" 집계(dropCount 는 자리·시드로 결정론)(가방 대신). 귀환 시 `ExpeditionResult`(JSON: 모은 것, 처음 본 블록 = 도감 신규). 마을 `storage(village,item,count)` 에 더한다(M4·M6 이 쓴다). 늦게 돌아오면 절반(`failedReturnKeepRatio`)
- [x] 프로토콜: 전환·정산은 JSON(`worldEnter`·`expeditionResult`·`expeditionState`·`startExpedition`·`returnHome`), 1Hz 타이머만 바이너리 `ExpeditionTimer 0x30`. 시드로 클라가 지형을 만들고 변경분(ChunkData·BlockChanged)만 받는다. 모두 돌아오면 원정 종료(다음 출발은 새 섬)
- [x] 테스트 9개: 시작·합류(같은 시드)·welcome 에 진행 상태, 세계별 브로드캐스트·드롭 집계·타이머 단계·경계, 포탈 귀환 정산·창고, 시간 종료 강제 귀환(절반 올림)·유예 폐기·새 시드, 끊김

### 4. 클라이언트 — 완료 2026-09-19 (패널 검증: 출발 → 섬 도착 → 블록 3개 → 포탈 귀환 → 정산 "발광석 가루 ×3, 조약돌 ×2")
- [x] 마을 포탈 문틀 안에 보라 막(`render/Portal.ts`, 블록이 아니라 그림 — 지형 버전 안 바뀜). 그 안에 서면 "원정 출발" 카드(초원 섬 · 10분 · 6분 뒤 밤 · 상자 3) → 요청. 원정 중이면 "따라가기 (N명, 남은 분)"
- [x] 세계 전환(`Game.ts WorldCtx`): 지형·조명·청크 렌더러·플레이어·상호작용을 통째로 바꾼다. 섬 생성 236ms + 조명 239ms(2코어 VM). 돌아올 때 마을을 시드로 다시 만들고 서버 변경분을 받는다
- [x] HUD: 상단 중앙 타이머(남은 시간·낮/저녁/밤, 3분 노랑·1분 빨강 깜빡), 3분·1분 경고 토스트, 낮→밤 `uSkyLight` + 하늘 돔·배경색. (나침반 포탈 방향은 5절 피드백 보고)
- [x] 정산 창: 모은 것 목록(아이콘·개수·한국어 이름 `rules/items.ts`), "한 번 더 갈까?"(바로 재출발) / "마을 구경하기". 도감 신규는 M6
- [x] 늦게 돌아온 경우 안내("시간이 다 되어 저절로 돌아왔어요. 절반만 가져왔어요")

### 5. 플레이테스트·마무리 — **M3 통과 2026-09-19** (아빠 "원정 확인했음")
- [x] 아빠 PC + 아들 폰 원정: 출발 버튼 두 가지 고장(PC 마우스 잠금, 폰 탭 필터) 고친 뒤 확인. 시간 10분·밤 6분은 그대로(바꾸자는 말 없으면 유지)
- [ ] `i` 창 수치(섬 생성 ms, 전환 중 끊김) → `PLAYTEST-LOG.md` (아직 미기록 — 폰에서 느리다는 말이 나오면 청크를 나눠 만들기)
- [x] 통과 → M4 착수 2026-09-19

## M4 작업 분해 (4주) — 착수 2026-09-19

목표: **아들이 JSON 을 고쳐 새 레시피를 만들고, 채팅 문구 20개 확정.** 블록은 이제 유한하다 — 부수면 가방에 들어오고, 놓으면 가방에서 나간다(마인크래프트 서바이벌, 결정 #66). 가방·제작·양조·채팅 전부 서버가 진실(규칙 1).

M4 에서 하지 않는 것: 아이템 엔티티(바닥에 떨어지는 것 — 버리기는 "사라짐"), 갑옷·왼손 칸(그림만, M7), 화로 연료(M4 는 연료 없이), 대장간(M6 건물), 도감·경험치(M6), 인챈트(v1.1).

### 1. 공용 규칙 (`shared/rules`) — 완료 2026-09-19
- [x] `inventory.ts`: 가방 = 핫바 10 + 가방 27 = 37칸, 한 칸 64개. 순수 함수 `give/take/move/countOf` (서버·클라 미리보기 공용), 결정론(빈 칸부터 채우고 같은 아이템 먼저 합침)
- [x] `recipes.ts`: `recipes.json` zod 로더(한국어 에러), station 별 목록, `canCraft(bag, recipe)`/`craft`. 모양 없는 레시피(재료 개수만 — 아들 JSON 그대로)
- [x] `potions.ts` 아이템 id: `potion.speed`, `potion.speed.long`, `potion.speed.strong`, `splash_potion.…`, `lingering_potion.…` ↔ PotionState 변환
- [x] `phrases.ts`: `phrases.json` 로더(이모지·문구 id)
- [x] 테스트 13개: 가방 합치기·넘침·옮기기, 레시피 파일 통과·만들기·부족·가득 참, 물약 id 라운드트립, 문구

### 2. 서버 — 완료 2026-09-19 (**라이브 서버에는 클라 3번이 끝난 뒤 함께 배포** — 지금 올리면 옛 클라의 블록 놓기가 전부 거절된다)
- [x] `inventories(token, village, json)` 저장. welcome 에 가방 37칸, 바뀐 칸만 `InvSlots 0x14`(바이너리), flush·퇴장·귀환 때 저장
- [x] 블록 놓기: 가방에 그 아이템(액체는 찬 양동이)이 있어야(없으면 `NO_ITEM` 9 거절), 놓으면 -1(양동이는 빈 양동이가 남음). 부수기: 드롭(dropCount 결정론)을 가방에, 가득한 액체는 빈 양동이로 떠냄. 가득 차면 `BAG_FULL` 안내. 원정 정산 = 원정 중 가방에 들어온 것(`gained`), 늦으면 그중 절반을 가방에서 잃는다
- [x] `craft`(JSON): station 확인(가방은 항상, 제작대·화로는 5칸 안), 재료 차감·결과 지급, 가득 차면 lost 안내. `brew`: 양조기 5칸 안, 병 1~3칸 + 재료 칸 → `PotionRegistry.brew`, 연료 블레이즈 가루 1 = 20번. `blocks.json` 에 `crafting_table` 블록 추가(그림은 판자 임시)
- [x] `InvMove 0x15`: 옮기기·합치기·나누기·맞바꾸기(실패하면 두 칸 상태를 다시 보내 바로잡음), `InvDrop 0x16`: 버리기(사라짐)
- [x] `Emote 0x40`: 이모지·문구 번호만(`phrases.json` 검증), 초당 1개, 같은 세계 사람 전부(나 포함)
- [x] 테스트 6개(`inventory.test.ts`) + 기존 테스트에 시험 키트(`giveItems`). 전체 178개

### 3. 클라이언트 — 완료 2026-09-19 (패널 검증: 시작 키트 핫바 표시 → 판자 놓기 32→31 → 원목 1 → 판자 4 제작 → 채팅 "나이스" 로그·말풍선 → 가방 화면·만들기 탭)
- [x] 핫바 = 가방 0~9 칸(아이콘 + 개수, 빈 칸 흐리게). 손 블록은 든 아이템(`itemToBlock`), 블록 아닌 아이템은 색 칩 + 이름 두 글자 아이콘(`ui/itemIcon.ts`)
- [x] 가방 화면(🎒 버튼 오른쪽 아래 항상, PC E 키): 27칸 + 핫바 10칸, 탭 → 탭으로 옮기기/합치기/맞바꾸기, "반만 옮기기" 토글, 버리기(두 번 눌러 확인). 갑옷·왼손 칸 자리는 M7 에서
- [x] 🔨 만들기 탭: 만들 수 있는 것 먼저, 재료 있음/부족 표시(원목 1/1), 몇 번 가능, 제작대·화로 5칸 안이면 그 레시피가 목록에 들어옴(0.5초마다 스캔)
- [x] ⚗️ 양조 탭(양조기 5칸 안에서만): 가방에서 병(물병·물약) 3개까지 + 재료 1개 탭으로 고르기, 결과 미리보기(`brew` 공용), 양조하기
- [x] 채팅: 💬(PC T 키) → 이모지 13 + 문구 20 시트 → 상대 머리 위 말풍선(하양) 2.5~3.5초 + 왼쪽 위 로그 5줄(8초). 내 것은 토스트
- [x] 원정 정산 창 = 원정 중 가방에 들어온 것(서버 `gained`)

### 4. 데이터·아들
- [ ] `recipes.json` 에 아들이 레시피 하나 추가 → 게임에 바로 나오는지(완료 기준). 잘못 적으면 한국어 에러가 로비에
- [ ] 채팅 문구 20개·이모지(13 → 12?) 아들 확정 → `phrases.json`
- [x] 첫 가방: **시작 키트**(`data/starter-kit.json`, 아빠 결정 #67) — 처음 한 번, 아들이 내용 조절

### 5. 플레이테스트
- [ ] 아빠·아들: 나무 캐서 판자 → 제작대 → 문 만들기 → 집에 달기. 원정 가서 상자 부수고 돌아와 가방 확인. 채팅 문구 주고받기
- [ ] 통과 시 M5(가족 연결 — 닉네임 + PIN 이어하기 #63 포함)

## M5 작업 분해 (4주) — 착수 2026-09-19

목표: **아들이 할 일 체크 → 아빠가 폰(또는 게임 내 HUD)에서 승인 → 한 판 더 열린다. 월요일 정산 확인.** 규칙 4(할 일 보상은 시간만)·5(기본 시간은 서버가 안 깎음)·6(닉네임 + PIN, 이메일은 부모만). 설계 정본 `docs/FAMILY-SYSTEM.md`, 값은 `data/family-rules.json`(아들 값: 평일 20/주말 30, 보너스 한도 5).

### 1. 닉네임 + PIN 이어하기 (`server/accounts.ts`, 결정 #63) — 완료·라이브 배포 2026-09-19 (패널 검증: 새 이름 → PIN 정하기 → 다른 토큰으로 같은 이름 → "이미 있는 이름" → 틀린 PIN 거절 → 맞는 PIN → 같은 토큰·같은 가방)
- [x] `accounts(nick_key, nick, token, pin_hash)`: 이름은 서버 전체 하나(공백·대소문자 무시). 첫 입장 때 내 토큰 것이 되고 welcome `needPin` → 로비가 PIN 정하기 창. PIN 은 scrypt 해시
- [x] 다른 기기에서 같은 이름 → `NICK_TAKEN` → PIN 창 → `resume{nick,pin}` → `resumed{token}` → 토큰 바꿔 저장하고 다시 join. 틀리면 이름당 10분에 5번
- [x] 테스트 5개: 첫 입장·재입장·다른 토큰 거절·PIN 정하기·이어하기·잠금·이름 바꾸기·저장. 전체 184개

### 2. 부모 계정·가족 (`server/family.ts`, HTTP `/family`) — 완료·라이브 배포 2026-09-19 (패널 검증: /family 가입 → 코드 표시 → 게임 "가족 연결" → 코드 + PIN → 부모 화면에 아이 이름)
- [x] `parents(email, pw_hash)`, `families(code)`, `children(nick_key → family)`, `parent_sessions`(쿠키 30일). 부모 가입·로그인 scrypt, 이메일당 5회/10분 잠금, 가족 코드 6자리
- [x] 아이 연결: 게임 방법 창 "가족 연결" → 가족 코드 + 내 PIN → `linkFamily` → 내 계정이 그 가족의 아이. welcome `family` 로 상태 표시. 부모가 플레이어이기도 한 경우(자기 계정을 부모로 표시)는 M5-3 승인 카드와 함께
- [x] `/family` 페이지 뼈대(`static/family.html` + `/api/family/me|signup|login|logout|unlink`): 로그인, 가족 코드, 아이 목록·끊기. 테스트 3개, 전체 187개

### 3. 할 일·승인 — 완료·라이브 배포 2026-09-19 (시간은 **표시만**, 제한은 4번에서 아빠가 "제한해" 하면 켠다 — `DV_ENFORCE_TIME=1`)
- [x] `todos`·`todo_logs`·`time_ledger`·`parent_players`: 제목(30자)·반복(매일/요일/한 번)·승인 필요·활성. 오늘 카드 계산은 순수 함수(`shared/rules/family.ts` buildTodayCard, family-rules.json 검증 포함, 테스트 9개)
- [x] 게임: 아이는 위 가운데 ⏱ 칩(남은 분·할 일 n/m) → 탭하면 "오늘" 창(남은 시간 = 기본 + 보너스/한도 ± 조정 − 쓴 시간, 할 일 목록 "했어요"). 체크 → 승인 필요 없으면 즉시 approved. 서버가 접속 중 1분마다 쓴 시간 누적 → `today` 로 갱신
- [x] 부모: `/family` 맨 위 승인 대기(승인/아직), 아이별 카드(남은 시간·오늘 할 일 상태·할 일 추가/끄기/삭제), 10초마다 자동 갱신 + 게임 HUD 승인 카드(부모 화면에서 "내 게임 이름 + PIN" 으로 부모 플레이어 연결 → 게임 중이면 `approvalAsk` 카드, 승인/아직/나중에). 승인 순간 아이 카드 갱신(보너스 ↑)
- [ ] 남은 것: 승인 순간 아이 화면 "+N분" 토스트(지금은 칩 숫자만 바뀜), 주간 한도는 5번 정산 전까지 첫 주 값(5분)

### 4. 시간 — 구현 완료 2026-09-20, **제한 켬 2026-09-20**(아빠 "제한해", `start-server.ps1` 의 `DV_ENFORCE_TIME=1`, 결정 #73)
- [x] `time_ledger(child, date, used_sec, manual_adj, no_play)` + `time_adjustments`: 접속 중 1분마다 used_sec +60(원정 중 포함), 5분 무입력(위치·시선·블록·가방·채팅 변화 없음) 자동 로그아웃(켜져 있을 때)
- [x] HUD(아이만): ⏱ 칩 남은 분, 5분·1분 토스트 경고(켜져 있을 때). 0 이면 `timeUp` → "오늘은 여기까지!" 화면 → 연결 종료. 원정 출발 조건 min(남은 시간, 차단까지) ≥ 원정 + 3분 — 포탈 카드 "오늘은 마을에서 놀자", 서버도 NOT_ENOUGH_TIME 으로 거절
- [x] 차단 시간대: 입장 거절 "지금은 게임 시간이 아니에요. 16:00 에 열려요", 접속 중 5분 전 토스트 → 진입 시 나가기. 부모 "오늘 게임 없음"(아이·날짜별, 켜져 있으면 접속 중인 아이도 바로 나감)
- [x] 부모 수동 조정(±120분, 사유 40자) — 아이 오늘 창에 노란 줄. 부모 자신에게는 규칙 없음. 부모 화면에서 아이 PIN 초기화
- [ ] 남은 것: 요일별 기본 시간·차단 시간대를 부모 화면에서 가족별로 바꾸기(지금은 `family-rules.json` 값), 아빠 "제한해" → 서버 환경 변수 켜고 재시작

### 5. 주간 정산 — 완료 2026-09-20 (결정 #74)
- [x] 순수 함수(`shared/rules/family.ts`): 주는 월요일 시작(서울), `lastWeekWindow`, `weeklyRate` = 승인 / 예정(매일·요일·승인 전 '한 번'을 날마다 셈, 할 일 만든 날 이전은 안 셈, 할 일 0개면 100%), 달성률 → 한도(`tiers`), 오늘 보너스 = min(한도, round(승인/예정 × 한도)). vitest
- [x] 서버 `week_settlements(child, week_start)`: (아이, 주) 당 한 번만 — 지난주 기록을 나중에 승인해도 이번 주 한도는 그대로. 시작할 때 보정 + 5분마다 확인(월요일 00:00 서울에 새 주가 되면 그때 만들어지고 접속 중인 아이 카드가 갱신됨). 첫 주(지난주에 연결·기록 없음)는 정산 없이 5분
- [x] 아이 오늘 창에 이번 주 문구("지난주 대단했어…"/첫 주 "처음이니까 믿고 시작할게"), 부모 카드에 "이번 주 한도 N분 — 지난주 달성률 %(승인/예정) · 그 전 주 %"

### 6. 플레이테스트·마무리 — 준비 완료 2026-09-20, 아빠·아들 실행 중
- [x] 준비: 승인 순간 아이 화면 "+N분! 할 일이 확인됐어요" 토스트, 체크리스트(`docs/PLAYTEST-LOG.md` M5 플레이테스트 체크리스트). 실제 가족 데이터 확인: 쁘뚜 할 일 2개(숙제 하기·목욕 바로하기, 확인 필요) 9/19·9/20 승인, 조정 +5, 부모 플레이어 오가니
- [x] 아빠 폰 `/family` 로 할 일 만들기 → 아들 체크 → 승인 → "+N분" → 조정 → 원정 다녀온 뒤 쓴 시간 증가 — **아빠 실접속 확인 2026-09-20**
- [x] 정산 메시지 확인 — 아빠 2026-09-20(서버 시계를 월요일로 돌려 확인). 아들 피드백 3줄은 받는 대로 PLAYTEST-LOG 에
- [x] 아빠 "제한해" 2026-09-20 → `start-server.ps1` 에 `DV_ENFORCE_TIME=1` → 재시작. 실접속 확인은 PLAYTEST-LOG
- [x] **M5 통과 2026-09-20** → M6(마을 성장 + 드래곤 + 경험치)

## M6 작업 분해 (5주) — 착수 2026-09-20

목표: **2주 뒤 접속한 친구가 "마을 커졌다"고 먼저 말한다. 아들이 경험치 레벨을 써서 첫 드래곤 알을 부화시키고, 안장을 얹어 타고, 빔을 쏜다.** 정본 `docs/XP-SYSTEM.md`(경험치)·`docs/DESIGN.md` 3절(드래곤 시스템)·`docs/DRAGON-SKILLS.md`·`data/xp.json`·`data/dragons.json`·`data/buildings.json`·`models/`. 규칙 4(할 일과 경험치 절대 연결 안 함)·1(경험치·드래곤·건물은 서버가 진실). 시간 제한은 M6 동안 꺼 둠(아빠).

### 1. 경험치·레벨 (`shared/rules/xp.ts`, `data/xp.json`) — 완료·라이브 2026-09-20
- [x] 순수 함수: 마인크래프트 Java 레벨 공식(`xpToNextLevel`·`totalXpForLevel`·`levelFromTotalXp`, 참고값 55/160/315/550/1395 vitest), `xp.json` 검증·로더(`XP`), 획득량 뽑기(범위는 자리·시드 결정론)
- [x] 서버: `players.xp_total`(토큰별, 정수), 획득 — 광석 캐기(`xp.json mining`)·원정 귀환 성공 10·보물 상자 열기 5(오두막 상자를 부수면 = 열기). 프로토콜 `XpGained 0x52`(양·자리)·`XpState 0x53`(총량·레벨·진행도, welcome·정정용)
- [x] 클라: 핫바 위 초록 경험치 바 + 가운데 레벨 숫자, 구슬이 튀어나와 날아오는 연출 + "딩"(음높이 흔들림)·레벨업 소리(Web Audio 합성, 파일 없음), 레벨 5/10/20/30 꾸미기 해제 토스트(모자·망토 그리기는 뒤로)
- [ ] 죽음 드롭·회수는 M7

### 2. 드래곤 알·둥지·부화 (`shared/rules/dragons.ts`, 서버 `dragons` 표)
- [ ] `dragons.json` 검증·로더(`DRAGONS`, 16종·재료·티어·스킬), 알 만들기 = 제작대 레시피처럼 `recipe` 재료 → 아이템 `dragon_egg.<id>`(가방 "만들기" 탭에 "드래곤 알" 묶음)
- [ ] 둥지 1단계(`buildings.json dragon_nest_1`, 4마리): 마을 광장 정해진 자리에 서버가 실제 블록 구조물로 짓는다(비용은 6번 창고에서 — 그 전엔 시작부터 지어져 있음). 둥지 "알 자리"에 알 놓기 → 부화 카드(필요 레벨·내 레벨) → `hatch` → 레벨 차감(`hatchLevelCostByTier`) → 아기 드래곤. 레벨이 모자라면 알은 둥지에 보관
- [ ] 드래곤은 개인 소유·영구: `dragons(id, token, dragon, stage, hatched_at, fed, resting_until)`. welcome 에 내 드래곤 목록, 도감 창(Tab / 📖)에 드래곤 탭 16칸(얻은 것만 색)

### 3. 드래곤 보이기·성장 (`client/render/DragonMesh`)
- [ ] `models/*.json` 복셀(아기·어른) → Three.js 메시. 나머지 13종은 `models/dragon-voxels.js` 생성기에 색 파라미터만 추가(아들 그림이 오면 교체)
- [ ] 둥지에서 서성임·숨쉬기·날개 살짝(간단 idle), 이름표(주인). 서버가 위치를 정하고(둥지 슬롯) 모두에게 보인다 — 친구가 들어와도 "쟤 드래곤 있네"
- [ ] 성장: 서버 시계로 `baseGrowMinutes` 60분 → 어른(크기 2배, 무섭게). 재료 먹이기(`feedWithRecipeMaterials`, 1개 = 10분 단축). 부화 경험치(티어×5)·성장 경험치(티어×3)

### 4. 안장·탑승
- [ ] 안장 레시피(가죽 5 + 철 3 — 아들 확인), 드래곤을 탭 → 탑승. 조작: 스틱 이동, 점프 = 상승, 웅크리기 = 하강, 타고 걷기 가능(몸집 커서 걸림). 서버: 탑승 상태·드래곤 위치 = 플레이어 위치, 원정에도 같이 간다(포탈 통과)
- [ ] 내리기 버튼. 낙하·충돌은 플레이어 물리 그대로(드래곤 히트박스는 M7 몹과 함께)

### 5. 빔 + 기본 공격(연출)
- [ ] 탑승 UI 스킬 버튼 1개(빔) + 기력 바. C→S `skill` → 서버가 기력·쿨타임 검증 → 브로드캐스트 → 색·세기별 빔 연출(24칸, 1.5초). 몹이 없으니 피해는 M7 에서 붙인다(블록은 안 부순다)
- [ ] 대표 스킬(signature) 1개는 M7 몹과 함께

### 6. 마을 성장 — 창고·건물·마을 레벨·도감
- [ ] 공유 창고: 광장 창고 건물의 "창고 상자"(마을 `storage` 표) — 가방 ↔ 창고 넣기/꺼내기 UI. 개인 기여도 표 없음(DESIGN 6절)
- [ ] 건물 짓기 메뉴(`buildings.json` cost → 창고에서 차감 → 서버가 정해진 자리에 구조물 생성): 창고·대장간·농장·등대·둥지 2/3. 마을 건물은 자동 보호(못 부숨)
- [ ] 마을 레벨 = 건물 수 + 도감 수 → 입구 표지판·깃발 변화. 도감(블록·드래곤) 처음 얻으면 +5 경험치·"새로 발견!" 토스트
- [ ] 아들 확인: 둥지·건물 자리(광장 어디), 안장 재료, 성장 시간 60분이 맞는지

### 7. 플레이테스트
- [ ] 아들: 원정 1~2판 → 레벨 1 → 나무 드래곤 알 → 부화 → 둥지에서 보기 → 먹이 → 안장 → 타고 날기 → 빔. 친구 접속해서 "마을 커졌다". 피드백 → PLAYTEST-LOG

## M3 이후 상세 분해는 각 마일스톤 시작 시 작성
M3 에 필요한 기획(원정지 6곳, 드래곤 16종 재료·티어, 시간 규칙)은 아들 1~7차 답변으로 갖춰졌다. **물약 양조(8차, `data/potions.json` + `shared/rules/potions.ts`)는 데이터·규칙 코드가 준비됨** — M4 에 양조기 UI 와 이동·시야 물약, M7 에 체력·전투 물약 효과(결정 #61). 남은 아들 질문은 `QUESTIONS-FOR-SON.md` "다음 회의에서 받을 것".
